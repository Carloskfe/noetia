# Noetia — Redis configuration

# Persistence (Append-Only File)
appendonly yes
appendfsync everysec

# Memory limits
maxmemory 256mb
maxmemory-policy noeviction

# Connection
bind 0.0.0.0
protected-mode no
port 6379

# Logging
loglevel notice
